{
  "name": "getra-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node ../scripts/dev-next.mjs backend 8080",
    "build": "next build",
    "start": "next start",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "next typegen && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "marked": "^18.0.10",
    "puppeteer-core": "^25.8.0",
    "sharp": "^0.35.3"
  }
}
