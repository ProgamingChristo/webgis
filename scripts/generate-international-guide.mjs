import puppeteer from 'puppeteer-core';
import { marked } from 'marked';
import { readFile, writeFile, mkdir, cp } from 'node:fs/promises';
import path from 'node:path';
const dir=path.resolve('docs/international-guide');
const destination=process.env.GETRA_DOCS_OUTPUT || 'D:/getra docs/Production docs/final/LAST FEATURE/50 feature/astra';
const features=JSON.parse(await readFile(path.join(dir,'features.json'),'utf8'));
const capture=JSON.parse(await readFile(path.join(dir,'capture-report.json'),'utf8'));
const base='https://getra-routing-api.tail0ed517.ts.net:8443';
const refs=[['Frontend',base],['Login',base+'/login'],['Backend health','https://getra-routing-api.tail0ed517.ts.net/api/health']];
const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const list=items=>`<ol>${items.map(s=>`<li>${esc(s)}</li>`).join('')}</ol>`;
const link=(url,label=url)=>`<a href="${esc(url)}">${esc(label)}</a>`;
const img=async(file,cls='shot')=>`<figure class="${cls}"><img src="data:image/${file.endsWith('.jpg')?'jpeg':'png'};base64,${(await readFile(path.join(dir,'screenshots',file))).toString('base64')}" alt="${esc(file)}"/></figure>`;
const pages=[];
function page(kicker,title,body,klass='') {pages.push({title,body:`<section class="page ${klass}"><header><div class="kicker">GETRA / ${esc(kicker)}</div><h1>${esc(title)}</h1></header><main>${body}</main><footer><span>Basemap & Global Data Center · 20 September 2026</span><b>${String(pages.length+1).padStart(2,'0')}</b></footer></section>`});}
const refBlock=`<div class="links">${refs.map(([name,url])=>`<p><b>${name}</b><br>${link(url)}</p>`).join('')}</div>`;
page('Panduan penggunaan · Produksi','Basemap & Global Data Center',`<div class="cover-title">Peta yang berganti.<br>Data yang dapat ditelusuri.</div><p class="intro">Panduan basemap, 20 layer internasional, sumber data, screenshot aktual, dan batasan penggunaan.</p>${await img('basemap-mapid.png','cover-shot')}<div class="cover-bottom"><strong>Edisi 20 September 2026</strong><p>GIS menghitung. Provider menyediakan fakta. AI menginterpretasikan hasil.</p><p>Dokumen ini membedakan fitur terhubung, sumber bermasalah, dan integrasi yang belum selesai.</p></div>`,'cover');
page('Mulai di sini','URL resmi & ruang lingkup',`${refBlock}<h2>URL dipertahankan</h2><p>Gunakan ketiga URL di atas sebagai referensi produksi. Port <b>8443</b> digunakan frontend dan login. Backend health menggunakan HTTPS tanpa port 8443.</p><h2>Isi panduan</h2><p>Dokumen ini membahas perbaikan basemap dan <b>20 fitur Global Data Center</b> pada pekerjaan terbaru. Nama folder “50 feature” adalah lokasi penyimpanan yang diminta, bukan klaim bahwa 50 fitur telah diverifikasi di audit ini.</p><div class="note"><b>Status yang perlu diketahui</b><p>MAPID, OSM, dan Esri telah diuji pada peta nyata. CARTO memerlukan key. Lima fitur memerlukan kredensial, tiga feed resmi belum terhubung, dan beberapa provider publik dapat gagal atau timeout.</p></div><h2>Cara membaca screenshot</h2><p>Gambar berasal dari aplikasi yang dijalankan melalui URL publik. Status dan jumlah record merupakan snapshot saat pengambilan, bukan janji ketersediaan di masa depan. Perbesar PDF untuk membaca detail kecil pada gambar.</p>`);
page('Navigasi dokumen','Daftar isi',`<div class="toc"><p><b>01–02</b> Sampul, URL produksi dan ruang lingkup</p><p><b>03</b> Daftar isi</p><p><b>04</b> Login dan akses halaman</p><p><b>05</b> Alur penggunaan Global Data Center</p><p><b>06–07</b> Pergantian basemap dan fallback</p><p><b>08</b> Mobile dan tablet</p><p><b>09</b> Rute, merchant dan Active Journey</p><p><b>10</b> Freshness, sumber dan AI</p><p><b>11–30</b> Panduan 20 fitur, satu fitur per halaman</p><p><b>31–32</b> Matriks status sumber aktual</p><p><b>33</b> Konfigurasi operator</p><p><b>34</b> Troubleshooting dan batasan</p><p><b>35</b> Bukti pengujian dan reproduksi dokumentasi</p></div><div class="note">Dokumentasi teknis lebih rinci tersedia dalam <b>GETRA_LAPORAN_TEKNIS_A-I.pdf</b> dan source <b>docs/INTERNATIONAL_DATA_REPORT.md</b>.</div>`);
page('Akses aplikasi','Login dan membuka fitur',`${await img('login.jpg','short-shot')}${list(['Buka URL Login yang tercantum di halaman 2.','Masukkan email dan password akun GETRA Anda, lalu tekan Masuk. Screenshot dokumentasi tidak menyertakan password.','Dari navigasi Global, buka Global Data Center; atau gunakan URL /international/weather secara langsung.','Untuk membuat rute, kembali ke Peta GETRA. Login/logout mengembalikan basemap ke MAPID.'])}<div class="note">Jangan memasukkan API key provider pada kolom login atau UI peta. Kredensial provider dipasang operator pada server.</div>`);
page('Alur utama','Dari lokasi ke data',`${await img('weather.jpg','shot')}${list(['Pilih Layer atau tab kelompok di bagian atas.','Klik peta untuk menentukan koordinat, isi Latitude/Longitude, atau tekan Gunakan pusat peta.','Atur radius serta filter yang tersedia, lalu tekan Muat data / Retry. Pergerakan peta saja tidak mengambil data baru.','Klik titik, cluster atau hasil pada panel kanan. Baca Source, Updated, Fetched, TTL dan License.','Gunakan Rute ke lokasi ini atau Interpretasi berbasis data bila tersedia.'])}`);
page('Basemap / 01','Ganti peta tanpa kehilangan konteks',`${await img('basemap-mapid.png','shot')}${list(['Buka kontrol Basemap pada peta utama atau panel Global Data Center.','Pilih MAPID, OpenStreetMap, Carto Light, Carto Dark atau Esri Satellite.','Tunggu LOADING berubah menjadi READY; perubahan harus terlihat pada peta.','Tekan Reset ke MAPID untuk kembali ke default, tanpa mereset koordinat atau melakukan reload halaman.'])}<p class="note">Kamera, rute dan source aplikasi dipulihkan pada instance MapLibre yang sama. Pilihan disimpan untuk reload tab; sesi/tab baru memakai MAPID.</p>`);
page('Basemap / 02','Provider, imagery dan fallback',`${await img('basemap-esri.png','shot')}<table><tr><th>Provider</th><th>Kondisi pada audit</th></tr><tr><td>MAPID / OpenStreetMap / Esri</td><td>Render nyata lulus; attribution terlihat.</td></tr><tr><td>CARTO Light / Dark</td><td>AUTH_REQUIRED; key belum tersedia.</td></tr></table><p>Jika muncul <b>Basemap gagal dimuat</b>, aplikasi kembali ke MAPID. Gunakan tombol <b>Gunakan MAPID Default</b> untuk mencoba lagi. CONFIGURED/PUBLIC pada kartu provider bukan pengganti READY pada transaksi render.</p><p class="note">CARTO anonim dapat mengembalikan watermark API KEY REQUIRED meski HTTP 200. Tampilan tersebut tidak dihitung sebagai basemap berhasil.</p>`);
page('Perangkat kecil','Mobile dan tablet',`<div class="mobile-pair">${await img('mobile.png','phone')}${await img('mobile-sheet.png','phone')}</div>${list(['Pada mobile, peta tampil di atas kontrol dan panel detail. Gulir halaman untuk filter dan hasil.','Tekan tombol Basemap di atas peta untuk membuka bottom sheet.','Pilih provider, lalu gunakan Tutup untuk menutup sheet. Periksa centang pilihan dan statusnya.','Pada tablet, kontrol dan peta tersusun dua kolom dengan detail di bawah.'])}<p class="note">Viewport uji: 390 × 844, 820 × 1180 dan 1440 × 1000. Tidak ditemukan horizontal page overflow pada pengujian tersebut.</p>`);
page('Peta utama','Rute, merchant dan Active Journey',`${await img('journey.png','shot')}${list(['Dari detail hasil data, pilih Rute ke lokasi ini. Koordinat dikirim ke perencana GETRA.','Tetapkan titik asal, pilih moda, lalu tunggu rute dihitung GIS.','Tekan Mulai Perjalanan untuk membuka Active Journey; lokasi perangkat diperlukan untuk navigasi.','Ganti basemap melalui kontrol Basemap. Rute dan status perjalanan harus tetap terjaga.'])}<div class="note">Rute nyata, selected merchant/popup, 107 marker merchant, dan satu skenario Active Journey telah diuji. Screenshot journey menggunakan GPS pengguna yang diemulasi khusus QA; geometri rutenya tetap berasal dari GIS.</div>`);
page('Membaca hasil','Sumber, freshness dan interpretasi AI',`<table><tr><th>Indikator</th><th>Makna dan tindakan</th></tr><tr><td>READY / LIVE</td><td>Provider merespons; baca jenis data dan waktu record, bukan hanya badge.</td></tr><tr><td>CURRENT / STALE</td><td>Umur data dibanding TTL; STALE tidak boleh dianggap kondisi saat ini.</td></tr><tr><td>STATIC / UNKNOWN</td><td>Inventory statis atau timestamp tidak dipublikasikan.</td></tr><tr><td>AUTH_REQUIRED</td><td>Kredensial server belum tersedia; hubungi operator.</td></tr><tr><td>UNAVAILABLE</td><td>Feed resmi belum terhubung atau kontrak tidak terpenuhi.</td></tr><tr><td>ERROR / OFFLINE</td><td>Provider/permintaan/koneksi gagal. Periksa lalu Retry.</td></tr><tr><td>0 hasil</td><td>Tidak ada record dalam kueri; bukan bukti tidak adanya kejadian/fasilitas.</td></tr></table><h2>Gunakan AI setelah data tersedia</h2>${list(['Muat dataset yang diperlukan.','Tekan Interpretasi berbasis data. Tool backend memakai kueri yang menghasilkan data tersebut, termasuk filternya.','Baca sumber, timestamp, TTL dan keterbatasan dalam jawaban. AI tidak mengganti hasil GIS atau angka provider.'])}<div class="note">Pertanyaan bernama tempat pada AI percakapan membutuhkan resolusi lokasi. Bila GeoNames belum aktif, koordinat tidak ditebak. Ringkasan saat ini bersifat deterministik, bukan analisis multi-layer sepanjang rute.</div>`);
for(const [index,f] of features.entries()) {
  const r=capture.features.find(r=>r.id===f.id);
  page(`Fitur ${String(index+1).padStart(2,'0')} / 20`,f.name,`<p class="lead">${esc(f.purpose)}</p><p class="route">${link(base+'/international/'+f.id,'/international/'+f.id)} <span>· ${esc(f.source)}</span></p>${await img(f.id+'.jpg','feature-shot')}<div class="caption">Screenshot publik: ${esc(r?.status??'CAPTURE ERROR')} · ${esc(r?.count??'?')} record · ${esc(r?.updated??'waktu sumber tidak dipublikasikan')}</div><div class="steps">${list(f.steps)}</div><div class="note compact">${esc(f.limit)}</div>`,'feature');
}
for(let part=0;part<2;part++) {
  const group=features.slice(part*10,part*10+10);
  page('Status aktual / '+(part+1),'Matriks sumber pada screenshot',`<p>Diambil: ${esc(new Date(capture.captured_at).toLocaleString('id-ID',{timeZone:'Asia/Jakarta'}))} WIB. Status berikut adalah snapshot pengambilan gambar, bukan klaim semua fitur complete.</p><table class="matrix"><tr><th>Fitur</th><th>Status / record</th><th>Catatan sumber</th></tr>${group.map(f=>{const r=capture.features.find(r=>r.id===f.id);return `<tr><td>${esc(f.name)}</td><td><b>${esc(r?.status??'CAPTURE ERROR')}</b><br>${esc(r?.count??'?')} record</td><td>${esc(r?.message??r?.provider??r?.capture_error??'Tidak tersedia')}${r?.truncated?'<br>Hasil dibatasi (truncated).':''}</td></tr>`;}).join('')}</table><p class="note">Perbedaan jumlah dari laporan audit sebelumnya adalah wajar untuk feed dinamis. Record tidak diisi ulang dengan fixture ketika provider gagal. Cuaca adalah model/prakiraan; OSM/DKI transit merupakan inventory, bukan sensor real-time.</p>`);
}
page('Operator','Konfigurasi sumber data',`<p>Kredensial disimpan di runtime server; tidak ada nilai secret di PDF, screenshot atau source dokumentasi.</p><table><tr><th>Environment</th><th>Runtime / kegunaan</th></tr>${[['MAPID_BASEMAP_KEY','Frontend server; sudah terpasang.'],['MAPID_STYLE_NAME','Frontend server; konfigurasi existing basic / Street Mapid.'],['CARTO_BASEMAP_API_KEY','Frontend server; belum tersedia.'],['NASA_FIRMS_MAP_KEY','Backend; aktivasi hotspot.'],['OPENAQ_API_KEY','Backend; aktivasi kualitas udara.'],['GEONAMES_USERNAME','Backend; geocoding, elevasi, timezone.'],['DKI_FLOOD_GEOJSON_URL','Backend; feed resmi pengamatan banjir.'],['DKI_DISASTER_GEOJSON_URL','Backend; dataset resmi BPBD.'],['BMKG_SATELLITE_MANIFEST_URL','Backend; manifest citra BMKG dengan bounds/waktu.']].map(([a,b])=>`<tr><td><code>${a}</code></td><td>${b}</td></tr>`).join('')}</table><h2>Setelah konfigurasi</h2>${list(['Pasang credential/feed resmi melalui pengelolaan environment deployment.','Restart/redeploy service terkait dan periksa backend health.','Jalankan kembali request API, parser, GIS, UI, freshness dan screenshot untuk source tersebut.','Hanya ubah status penerimaan setelah data nyata dapat diverifikasi.'])}<p class="note">Contoh environment berada di .env.example dan frontend/.env.example. Jangan menggunakan NEXT_PUBLIC_ untuk key provider baru.</p>`);
page('Troubleshooting','Jika hasil tidak seperti yang diharapkan',`<table><tr><th>Gejala</th><th>Tindakan</th></tr><tr><td>Basemap gagal</td><td>Gunakan MAPID Default, periksa koneksi dan status key provider.</td></tr><tr><td>AUTH_REQUIRED</td><td>Aktifkan credential di server; tidak dapat diperbaiki dengan mengganti lokasi.</td></tr><tr><td>0 hasil</td><td>Periksa radius, kategori, tanggal dan coverage; jangan menganggap tidak ada kejadian di dunia nyata.</td></tr><tr><td>Overpass timeout</td><td>Tunggu lalu Retry, gunakan area lebih kecil; ada cache dan satu mirror fallback.</td></tr><tr><td>GBFS belum ada titik</td><td>Muat katalog, pilih sistem, lalu muat feed. Catalog presence tidak menjamin operator online.</td></tr><tr><td>STALE</td><td>Periksa Updated/TTL; muat ulang jika perlu. Jangan menginterpretasikan sebagai data saat ini.</td></tr><tr><td>Service area tidak tersedia</td><td>Periksa cakupan dan respons GIS. Acceptance geometri service area belum lulus pada audit ini.</td></tr></table><h2>Batas implementasi yang masih terbuka</h2><p>Pricing/geofencing GBFS, seluruh variasi feed operator, GTFS/jadwal keberangkatan, KRL/bandara DKI, registry/cache terdistribusi, dan gabungan layer internasional ke seluruh peta utama belum selesai. Skenario live CCTV/sensor/traffic/community belum seluruhnya diterima dalam pengujian pergantian basemap.</p><div class="note">Flood, disaster dan satellite belum memiliki feed terverifikasi. Radar BMKG dapat 522/timeout. Panduan ini tidak menyatakan semua 20 fitur sudah complete.</div>`);
page('Validasi & reproduksi','Bukti pengujian dan file pendamping',`<table><tr><th>Pengujian</th><th>Hasil audit implementasi</th></tr><tr><td>Frontend / backend build</td><td>PASS; kedua container healthy.</td></tr><tr><td>TypeScript</td><td>Frontend dan backend PASS secara eksplisit.</td></tr><tr><td>Backend tests</td><td>463 tests / 15 files PASS.</td></tr><tr><td>Frontend tests terkait</td><td>Basemap, halaman internasional, route layer dan safety PASS.</td></tr><tr><td>Browser publik</td><td>MAPID/OSM/Esri, kamera, route, merchant, journey, reset, login/logout, mobile sheet dan 20 URL diuji.</td></tr><tr><td>Keamanan key</td><td>26 script client dan style diperiksa; key MAPID tidak ditemukan.</td></tr></table><h2>File yang disediakan</h2><p><b>GETRA_BASEMAP_GLOBAL_DATA_CENTER.pdf</b> — panduan ini.<br><b>GETRA_LAPORAN_TEKNIS_A-I.pdf</b> — laporan teknis lengkap.<br><b>screenshots/</b> — gambar asli.<br><b>capture-report.json</b> — waktu, URL dan status pengambilan.<br><b>README.md</b> — panduan versi GitHub.</p><h2>Reproduksi</h2><pre>node scripts/capture-international-guide.mjs\nnode scripts/generate-international-guide.mjs</pre><p>Gunakan Node.js/dependency repo dan Chrome. GETRA_DOCS_OUTPUT dapat mengganti folder tujuan. Proses pengambilan memakai provider asli dan dapat memerlukan beberapa menit.</p><p class="note">Hasil audit lengkap beserta batasannya adalah bagian dari dokumentasi. Full-repo lint masih memiliki kegagalan existing di luar perubahan ini.</p>`);
const css=`@page{size:A4;margin:12mm}*{box-sizing:border-box}body{margin:0;background:#fff;color:#173344;font:10pt/1.45 'Segoe UI',Arial,sans-serif}a{color:#087f94;text-decoration:none;overflow-wrap:anywhere}.page{background:#fff;width:186mm;height:272mm;position:relative;break-after:page;overflow:hidden;padding-bottom:12mm}.page:last-child{break-after:auto}header{border-top:2.3mm solid #008ba1;padding-top:4mm;margin-bottom:4mm}.kicker{font-size:8pt;letter-spacing:1.7px;color:#087f94;text-transform:uppercase;font-weight:700}h1{font-size:22pt;line-height:1.12;margin:2mm 0 0;letter-spacing:-.5px}h2{font-size:13pt;margin:6mm 0 2mm}p{margin:2.3mm 0}ol{margin:3mm 0;padding-left:6mm}li{margin:2mm 0;padding-left:1mm}.note{background:#eff7f8;border-left:1mm solid #008ba1;padding:3mm 4mm;margin-top:4mm;font-size:9pt}.compact{font-size:8.5pt;padding:2.4mm 3mm;margin-top:3mm}.links{padding:3mm 4mm;background:#f1f6fa;border-radius:3mm}.links p{margin:3mm 0}.shot,.short-shot,.feature-shot,.cover-shot{margin:0;display:flex;align-items:center;justify-content:center;background:#f5f8fa;border:1px solid #d9e6ec;border-radius:2mm;padding:1.2mm}.shot{height:137mm}.short-shot{height:135mm}.feature-shot{height:121mm}.shot img,.short-shot img,.feature-shot img,.cover-shot img{max-width:100%;max-height:100%;object-fit:contain}.caption{font-size:7pt;color:#5b7182;margin:1.4mm 0 2mm}.lead{font-size:10.5pt;margin:0 0 1mm}.route{font-size:7.5pt;margin:0 0 3mm}.steps{font-size:9pt;line-height:1.37}.steps li{margin:1.8mm 0}.feature h1{font-size:20pt}footer{position:absolute;bottom:0;left:0;right:0;border-top:1px solid #d2dfe6;padding-top:2.3mm;display:flex;justify-content:space-between;color:#526b7b;font-size:7.5pt}footer b{color:#008ba1}.cover header{border-top-width:3mm}.cover-title{font-size:28pt;line-height:1.13;font-weight:700;margin:9mm 0 5mm;color:#073b50}.intro{font-size:12pt;max-width:165mm}.cover-shot{height:120mm;margin-top:7mm}.cover-bottom{margin-top:6mm;font-size:10pt}.mobile-pair{display:flex;gap:5mm;justify-content:center;height:157mm}.phone{margin:0;height:100%;width:47%;display:flex;justify-content:center}.phone img{max-height:100%;max-width:100%;object-fit:contain}.toc{padding:2mm 0;font-size:11pt}.toc p{padding:2.6mm 0;border-bottom:1px solid #e4edf2}.toc b{display:inline-block;width:25mm;color:#008ba1}table{width:100%;border-collapse:collapse;font-size:9pt;margin:4mm 0}th{background:#0b4f65;color:white;text-align:left}th,td{padding:2.5mm 2.7mm;border-bottom:1px solid #d6e2e9;vertical-align:top}tr:nth-child(even)td{background:#f3f7fa}.matrix{font-size:8.4pt}.matrix th,.matrix td{padding:3.1mm 2.3mm}code,pre{font-family:Consolas,monospace;font-size:8pt;overflow-wrap:anywhere}pre{background:#eef4f7;padding:4mm;white-space:pre-wrap}`;
const html=`<!doctype html><html lang="id"><head><meta charset="utf-8"><title>GETRA · Basemap & Global Data Center</title><style>${css}</style></head><body>${pages.map(p=>p.body).join('')}</body></html>`;
await mkdir('outputs/international-guide-preview',{recursive:true});
await writeFile('outputs/international-guide-preview/guide.html',html);
let md=`# GETRA — Panduan basemap dan Global Data Center\n\nPanduan 20 fitur internasional dan basemap. Screenshot berasal dari URL publik; status merupakan snapshot ${capture.captured_at}.\n\n## URL produksi\n\n${refs.map(([n,u])=>`- ${n}: [${u}](${u})`).join('\n')}\n\n[Unduh panduan PDF](GETRA_BASEMAP_GLOBAL_DATA_CENTER.pdf) · [Laporan teknis PDF](GETRA_LAPORAN_TEKNIS_A-I.pdf) · [Laporan teknis sumber](../INTERNATIONAL_DATA_REPORT.md)\n\n## Cara memakai Global Data Center\n\n1. Login untuk memakai alur peta utama dan perjalanan. Buka Global → Global Data Center.\n2. Pilih layer, lokasi, radius dan filter.\n3. Tekan **Muat data / Retry**, lalu klik titik atau hasil.\n4. Periksa sumber, timestamp, TTL, license dan freshness.\n5. Gunakan **Rute ke lokasi ini** atau **Interpretasi berbasis data** bila tersedia.\n\n## Basemap\n\nBuka Basemap, pilih provider, tunggu READY. **Reset ke MAPID** kembali ke default tanpa page reload. MAPID/OSM/Esri diuji nyata; CARTO memerlukan key. Reload mempertahankan pilihan; login/logout/sesi baru kembali ke MAPID.\n\n![Basemap MAPID](screenshots/basemap-mapid.png)\n\n## Panduan per fitur\n`;
for(const f of features){const r=capture.features.find(r=>r.id===f.id);md+=`\n### ${f.name}\n\n[${base}/international/${f.id}](${base}/international/${f.id})\n\n${f.purpose}\n\nSumber: **${f.source}**. Snapshot: **${r?.status??'CAPTURE ERROR'}**, ${r?.count??'?'} record.\n\n${f.steps.map((s,i)=>`${i+1}. ${s}`).join('\n')}\n\n**Batasan:** ${f.limit}\n\n![${f.name}](screenshots/${f.id}.jpg)\n`;}
md+='\n## Batasan penerimaan\n\nLima fitur masih membutuhkan kredensial; flood/disaster/satellite belum terhubung; radar dan Overpass dapat gagal. Service area belum memenuhi acceptance geometri live. Pricing/geofencing GBFS dan beberapa integrasi lanjutan belum selesai. Dokumentasi tidak mengklaim semua fitur complete.\n\n## Reproduksi dokumentasi\n\n```powershell\nnode scripts/capture-international-guide.mjs\nnode scripts/generate-international-guide.mjs\n```\n\nFolder default PDF: `D:\\getra docs\\Production docs\\final\\LAST FEATURE\\50 feature\\astra`. Source, PDF, screenshot dan capture-report.json juga disimpan pada direktori ini di GitHub.\n';
await writeFile(path.join(dir,'README.md'),md);
const browser=await puppeteer.launch({executablePath:process.env.CHROME_PATH || 'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true,args:['--no-sandbox']});
try {
  const page=await browser.newPage();await page.setViewport({width:900,height:1150,deviceScaleFactor:1});await page.setContent(html,{waitUntil:'load'});await page.evaluate(()=>document.fonts.ready);
  const layout=await page.evaluate(()=>[...document.querySelectorAll('.page')].map((e,index)=>({page:index+1,title:e.querySelector('h1').textContent,overflow:e.scrollHeight>e.clientHeight+2,contentBottom:e.querySelector('main').getBoundingClientRect().bottom,footerTop:e.querySelector('footer').getBoundingClientRect().top})));const failures=layout.filter(p=>p.overflow||p.contentBottom>p.footerTop-2);
  if(failures.length)throw new Error('PDF content overlaps footer: '+JSON.stringify(failures));
  const allImagesLoaded=await page.evaluate(()=>[...document.images].every(i=>i.complete&&i.naturalWidth>0));
  if(!allImagesLoaded)throw new Error('A manual screenshot did not load');
  const pdf=path.join(dir,'GETRA_BASEMAP_GLOBAL_DATA_CENTER.pdf');await page.pdf({path:pdf,printBackground:true,preferCSSPageSize:true,tagged:true,outline:true});
  await mkdir('outputs/international-guide-preview',{recursive:true});for(const index of [0,5,10,20,32])await page.$$('.page').then(elements=>elements[index].screenshot({path:`outputs/international-guide-preview/page-${index+1}.png`}));
  const technical=(await readFile('docs/INTERNATIONAL_DATA_REPORT.md','utf8')).replace(/\]\(international-guide\/([^)]*)\)/g, ']($1)');
  const technicalHtml=`<!doctype html><html lang="id"><head><meta charset="utf-8"><title>GETRA · Laporan teknis A–I</title><style>@page{size:A4 landscape;margin:13mm}body{font:10pt/1.5 'Segoe UI',Arial;color:#193447}h1,h2,h3{color:#07526a;break-after:avoid}h1{font-size:21pt}h2{border-top:2px solid #008ba1;padding-top:5mm;margin-top:8mm}table{border-collapse:collapse;width:100%;font-size:7.4pt}th,td{border:1px solid #d0dfe6;padding:2mm;overflow-wrap:anywhere}th{background:#e8f4f6}tr{break-inside:avoid}thead{display:table-header-group}a{color:#007a93;overflow-wrap:anywhere}code{font-size:8pt}pre{white-space:pre-wrap;background:#eef5f7;padding:4mm}p,li{orphans:3;widows:3}</style></head><body>${marked.parse(technical)}</body></html>`;
  await page.setContent(technicalHtml,{waitUntil:'load'});await page.pdf({path:path.join(dir,'GETRA_LAPORAN_TEKNIS_A-I.pdf'),printBackground:true,preferCSSPageSize:true,tagged:true,outline:true});
  await writeFile(path.join(dir,'pdf-validation.json'),JSON.stringify({generated_at:new Date().toISOString(),manualPages:pages.length,layoutChecks:layout.map(({page,title,overflow})=>({page,title,overflow})),allImagesLoaded},null,2));
  await mkdir(destination,{recursive:true});for(const name of ['GETRA_BASEMAP_GLOBAL_DATA_CENTER.pdf','GETRA_LAPORAN_TEKNIS_A-I.pdf','README.md','features.json','capture-report.json','pdf-validation.json'])await cp(path.join(dir,name),path.join(destination,name));await cp(path.join(dir,'screenshots'),path.join(destination,'screenshots'),{recursive:true});await cp(path.join(dir,'evidence'),path.join(destination,'evidence'),{recursive:true});
  console.log(JSON.stringify({pages:pages.length,pdf,destination,layout:'PASS'}));
}finally{await browser.close();}
