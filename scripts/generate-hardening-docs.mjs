import { readFile, writeFile, mkdir, cp } from 'node:fs/promises';
import { execFileSync } from 'node:child_process';
import { marked } from 'marked';
import puppeteer from 'puppeteer-core';
const root='docs/production-hardening';
const dest='D:/getra docs/Production docs/final/LAST FEATURE/50 feature/astra';
await mkdir(`${root}/evidence`,{recursive:true});await mkdir(`${root}/screenshots`,{recursive:true});await mkdir(dest,{recursive:true});
const read=async p=>JSON.parse(await readFile(p,'utf8'));
const optional=async p=>{try{return await read(p);}catch{return null;}};
const release=await optional('outputs/production-hardening/release-runtime.json')||{release_sha:'2f0b78a',build_status:'PENDING',candidate_status:'PENDING',promoted:false};
const canonical='https://getra-routing-api.tail0ed517.ts.net:8443';
const api=await optional('outputs/production-hardening/vm-provider/api-report.json')||await read('outputs/international-audit/public/api-report.json');
const ai=await read('outputs/production-hardening/ai-final/results.json');
const baseline=await read('outputs/production-hardening/ai-live/baseline-results.json');
const responsive=await optional('outputs/production-hardening/vm-responsive/report.json')||await read('outputs/production-hardening/responsive/report.json');
const accessibility=await optional('outputs/production-hardening/vm-accessibility/report.json')||await read('outputs/production-hardening/accessibility/report.json');
const security=await optional('outputs/production-hardening/vm-security/report.json')||await read('outputs/production-hardening/security/report.json');
const camera=await read(`${root}/camera-verification.json`);
const features=await read('docs/international-guide/features.json');
const publicAudit=await optional('outputs/production-hardening/public-runtime/report.json');
const date=new Date().toISOString();
const dependencyAudit=await optional('outputs/production-hardening/dependency-audit-final.json');
const dependencyAuditAll=await optional('outputs/production-hardening/dependency-audit-all-final.json');
const row=r=>`| ${r.layer}${r.query?.adm4?' (BMKG)':''} | ${r.source??'Tidak tersedia'} | ${r.status??'ERROR'} | ${r.count??'—'} | ${r.ms} ms |`;
const matrix=`| Layer | Sumber | Status API | Record | Latensi |\n|---|---|---|---:|---:|\n${api.results.map(row).join('\n')}`;
const blockers=[
 'CARTO Light/Dark: kredensial belum tersedia; fallback MAPID teruji, tetapi tile CARTO belum lolos uji nyata.',
 'NASA FIRMS, OpenAQ, GeoNames: kredensial belum tersedia. Fire, air-quality, places, elevation, timezone mengembalikan AUTH_REQUIRED.',
 'Flood, disaster dan satellite: kontrak feed resmi belum terhubung. Radar/Overpass dapat gagal di penyedia; tidak diganti data contoh.',
 'CCTV AI: tidak ada input frame ke layanan inference. Seluruh hitungan, FPS, model dan bounding box harus tetap tidak tersedia.',
 'Sensor: 9 entri inventory, 0 sensor dengan observasi runtime terverifikasi; tidak boleh dinyatakan sebagai sensor aktif.',
 'Deployment publik berubah oleh pekerjaan paralel. Audit publik masih menemukan canvas sintetis dengan klaim YOLOv8/TensorRT dan hitungan objek; kandidat menghapusnya tetapi belum dipromosikan. Bukti kandidat bukan bukti versi publik.',
 'Belum ada audit manual WCAG menyeluruh, pengukuran LCP/INP lapangan, atau pengujian payload lengkap untuk setiap adapter.',
 'Skor hallucination/groundedness 154 jawaban belum diukur dengan review klaim per klaim. Full-screen mobile dan pengujian transaksi pembayaran produksi belum lengkap.'
];
const common=`Dibuat ${date}. Kandidat kode: \`${release.release_sha}\`. Branch: \`fix/production-hardening-20260920\`. Status penerimaan keseluruhan: **NOT_READY**. Build: **${release.build_status}**.\n\nFrontend: ${canonical}\n\nLogin: ${canonical}/login\n\nBackend health: https://getra-routing-api.tail0ed517.ts.net/api/health\n\nURL ini tetap dipertahankan. Laporan membedakan kandidat dari deployment publik.\n`;
const docs={
'ARCHITECTURE.md':`# Arsitektur GETRA — hardening\n\n${common}\n## Alur aplikasi\n\nBrowser Next.js/MapLibre → route frontend /api/international → backend Next.js → adapter provider → validasi GeoJSON/provenance → cache TTL proses → hasil peta dan interpretasi deterministik. API auth, merchant, promo dan community memakai kebijakan endpoint serta bearer token yang sudah ada. Valhalla menjadi engine jaringan jalan; GIS tetap menghitung geometri, jarak dan waktu.\n\n## Kepemilikan layer peta\n\nMapLayerRegistry menyimpan id, source, jenis, visibility, dependency, reload/destroy dan pemeriksaan isLoaded. Snapshot sumber/layer aplikasi direhidrasi menurut urutan dependency sesudah setStyle; siklus dan dependency hilang dilaporkan, bukan diabaikan. Instance MapLibre, kamera, popup merchant dan state Journey dipertahankan. Marker DOM bukan layer style dan tetap dikelola pemiliknya.\n\nMAPID adalah default; login/logout/reset mengembalikannya. Pilihan basemap pertama setelah login kini membentuk penanda sesi sebelum menyimpan pilihan. Error tile dari source style lama diabaikan. Kredensial basemap yang tidak tersedia memicu fallback terjelaskan.\n\n## Data dan batas sistem\n\nRegistry sumber dan cache berada di memori proses, bukan database persistensi. Cache maksimal 200 kueri dengan deduplikasi in-flight. Resilience per host maksimal 200 entri, tiga percobaan, backoff 250/500 ms dan total anggaran 30 detik; circuit membuka 60 detik setelah tiga kegagalan yang dapat dicoba ulang. Auth/404/429 tidak dicoba ulang. Status sumber dan quality terpisah.\n\nService area mencoba RPC jaringan yang tersedia lalu Valhalla pedestrian isochrone jika RPC belum menghasilkan geometri. Kontur ditandai NETWORK_ISOCHRONE; jumlah edge/node tetap null karena tidak dihitung. Hasil bukan radius lingkaran.\n\nCCTV registry dibagi frontend/backend lewat types/cctv-registry.ts. Player hanya memuat embed kamera terpilih. Sensor dan inference memiliki status terpisah dari keberhasilan player.\n`,
'DATA_SOURCES.md':`# Sumber data dan mutu\n\n${common}\n## Hasil permintaan sumber nyata\n\nBase pengujian: ${api.base}. Mulai: ${api.started}; selesai: ${api.finished}. HTTP 200 dapat membawa AUTH_REQUIRED/UNAVAILABLE/ERROR dan tidak berarti data tersedia. API LIVE berarti respons sumber memenuhi jalur normalisasi; UI menampilkannya sebagai data sumber dimuat. Prakiraan dan inventory bukan sensor live.\n\n${matrix}\n\n## Konfigurasi operator\n\nNASA_FIRMS_MAP_KEY, OPENAQ_API_KEY dan GEONAMES_USERNAME disediakan melalui secret environment backend. Jangan menyimpan nilai di repository, browser atau PDF. CARTO memakai konfigurasi basemap server yang ada. Flood/disaster/satellite memerlukan endpoint resmi dengan skema dan izin yang dapat diverifikasi. URL arbitrary tidak diterima sebagai query pengguna.\n\nRegistry /api/international/sources menyertakan provider, dataset, URL, jenis, coverage, license, refresh interval, credential name, status serta last_success/last_failure. Timestamp awal null. Observasi mempunyai name, source, dataset, timestamp, license dan freshness. Timestamp pengambilan tidak menggantikan timestamp observasi.\n\nValidator menolak koordinat di luar WGS84, geometry rusak, field provenance hilang dan timestamp invalid/future di luar toleransi. Quality memuat accepted/rejected, completeness, freshness, timestamp validity; source reliability tetap PROVIDER_REPORTED, bukan skor rekaan. Nol hasil adalah respons kosong, bukan angka sensor nol. STALE tetap dapat dibaca dengan label.\n\nTidak semua feed menyatakan lisensi redistribusi. Metadata lisensi mengikuti provider dan ketidakjelasannya tetap ditulis.\n`,
'API_CONTRACTS.md':`# Kontrak API\n\n${common}\n## GET /api/international/:layer\n\nLayer: weather, earthquakes, active-fire, air-quality, places, elevation, timezone, poi, accessibility, water-refill, bikeshare, micromobility, ev-charging, transit-stops, jakarta-transit, flood, disaster, weather-radar, weather-satellite, open-data.\n\nlat/lon wajib numerik WGS84; radius meter. Filter menurut layer: q, category, magnitude, adm4, system, source, year, points, since, until. Contoh: /api/international/earthquakes?lat=-6.2&lon=106.82&radius=20000000&magnitude=2.\n\nRespons: layer, status, message, source, fetched_at, last_updated, ttl, data FeatureCollection, warnings, truncated, quality, cache_status. imagery dan systems bersifat opsional. Jangan menyamakan record inventaris, prakiraan, kontur dan pengukuran realtime.\n\nGET /api/international/sources tidak membutuhkan koordinat. Endpoint sengaja publik untuk data publik, dengan CORS allowlist dan rate limit spatial bersama. x-request-id dipropagasi, cache HTTP no-store; cache provider internal tetap TTL.\n\n## POST /api/international/interpret\n\napplication/json: {"layer":"weather","query":{"lat":-6.2,"lon":106.82,"radius":1000}}. Maksimum body 16.000 byte, termasuk stream tanpa content-length. Respons answer/source/status/generated_by. Interpretasi deterministik hanya memakai hasil tool.\n\n400: input malformed/koordinat invalid. 404: layer tidak dikenal. 413: body terlalu besar. 429: kuota. Kegagalan upstream dipetakan ke status dataset ERROR/AUTH_REQUIRED/UNAVAILABLE, tanpa mengisi angka dummy.\n\n## Service area dan AI\n\nService area existing POST /api/spatial/service-area mempertahankan autentikasi dan kontrak envelope. Fallback memberi analysis_method NETWORK_ISOCHRONE, source VALHALLA_OSM, MultiLineString, retrieved_at dan limitation flags. reachable_edge_count/reachable_node_count null bila tidak dihitung.\n\n/api/ai/ask menerima question, active_experience, context. Intent router merencanakan tool; hasil backend/GIS tetap otoritatif. Tidak ada tindakan admin otomatis dari kalimat pengguna.\n\n## Bukti kontrak\n\n180 kasus shared provider boundary (20 layer × 9 kondisi) memakai adapter mock: 401/403/404/429/500, timeout, malformed, empty, stale. Tes normalisasi provider terpisah memakai fixture terbatas. Ini belum memenuhi matriks payload penuh semua adapter; smoke real-data memakai HTTP sumber sebenarnya.\n`,
'CCTV.md':`# CCTV, AI Vision dan sensor\n\n${common}\n## Bukti kamera\n\n20 entri kamera. ${camera.results.filter(r=>r.http===200).length} embed spesifik diverifikasi dari portal resmi pada ${camera.checked_at}; tiga entri belum memiliki preview terverifikasi. Sumber: ${camera.portal}. HTTP 200 membuktikan endpoint dapat diambil, bukan kesehatan seluruh stream. Browser kandidat memeriksa video di dalam iframe; rincian readyState, dimensi dan currentTime ada pada evidence/responsive.json.\n\nPlayer nyata memakai allowlist jakcctv.jakarta.go.id, dki-jkt.balitower.co.id:7028 dan cctv-jsc.balitower.co.id:8011. Registry health tetap UNKNOWN sampai ada pemeriksaan berkala; last_frame_at tidak dibuat dari waktu halaman dibuka.\n\n## Cara memakai\n\n1. Buka ${canonical}/international/cctv.\n2. Pilih tab Live CCTV. Cari nama kamera atau gunakan Filter wilayah/provider/status.\n3. Tekan nama kamera. Preview hanya memuat kamera terpilih; tombol layar penuh memperbesar player.\n4. Jika preview gagal, pilih Sumber resmi. Periksa label UNKNOWN/NO_STREAM dan waktu verifikasi.\n5. Tab AI Vision menampilkan UNAVAILABLE ketika tidak ada inference terhubung. Video yang terlihat tidak membuktikan deteksi AI.\n6. Tab Sensors menampilkan inventory dan DATA UNAVAILABLE bila tidak ada observasi resmi.\n\n## Perubahan integritas\n\nDihapus: metrik FPS/latensi/model/hitungan statis, synthetic bounding boxes dan klaim inference LIVE. Script update-registry-ai dinonaktifkan. Tidak ada model computer vision produksi yang diaktifkan oleh perubahan ini. Nilai sensor dan waktu verifikasi tidak dibuat-buat.\n\nLicense dan kebijakan privasi mengikuti penyedia. GETRA tidak mengklaim masking runtime yang belum dilakukan. Embed resmi tidak memberi hak untuk mengunduh stream privat atau melatih model.\n`,
'INTERNATIONAL_FEATURES.md':`# Panduan penggunaan 20 fitur\n\n${common}\n## Langkah umum\n\nBuka /international, pilih kategori/layer, klik peta atau isi latitude/longitude, atur radius/filter lalu tekan Muat data / Retry. Periksa Data & sumber, timestamp, TTL, coverage, quality dan license. Klik titik atau baris hasil untuk detail; Rute ke lokasi ini menyerahkan koordinat ke GIS GETRA. Interpretasi berbasis data merangkum query yang benar-benar dimuat.\n\nGunakan Basemap untuk MAPID/OSM/CARTO Light/CARTO Dark/Esri Satellite. Fallback akan dijelaskan jika provider tidak tersedia. Reset ke MAPID mengembalikan default. Mobile memiliki kontrol bertumpuk dan drawer basemap; mode fullscreen peta GDC belum lengkap.\n\nAUTH_REQUIRED: operator perlu melengkapi kredensial. ERROR/OFFLINE: retry setelah koneksi/sumber pulih. STALE: baca waktu sumber sebelum menggunakan. UNAVAILABLE: jangan menafsirkan kekosongan sebagai kondisi aman.\n\n${features.map((f,i)=>`## ${i+1}. ${f.name}\n\n${canonical}/international/${f.id}\n\n${f.purpose}\n\n${f.steps.map((s,j)=>`${j+1}. ${s}`).join('\n')}\n\nBatas: ${f.limit}`).join('\n\n')}\n\n## Rute, Journey dan usaha\n\nPada /app tentukan asal/tujuan dan moda jalan kaki, motor atau mobil; hasil bergantung cakupan Valhalla. Mulai Journey memakai izin lokasi dan pembaruan GPS. Uji otomatis memakai GPS emulasi, bukan perjalanan fisik. Area jalan kaki menampilkan kontur jaringan bila graph RPC tidak tersedia.\n\n/umkm menyediakan daftar/detail usaha. Klaim kepemilikan harus diverifikasi sebelum mengubah data atau mengaktifkan promosi. /umkm/advertising mengelola kampanye menurut izin; pembayaran nyata tidak dijalankan pada audit ini. /community memakai autentikasi dan moderasi yang ada. Halaman konsep lama berlabel SIMULATION dan memerlukan opt-in eksplisit; bukan feed produksi.\n`,
'AI_EVALUATION.md':`# Evaluasi AI berbasis bukti\n\n${common}\n## Metode\n\n154 pertanyaan dalam 14 kategori terdapat di ai-questions.json. Baseline dan hasil memakai /api/ai/ask yang benar-benar berjalan, login akun QA ordinary, konteks Jakarta dan tool data nyata jika terkonfigurasi. Mode benchmark menaikkan rate limit hanya pada development.\n\n| Metrik | Baseline | Kandidat |\n|---|---:|---:|\n| HTTP 200 | ${baseline.metrics.successful_http}/154 | ${ai.metrics.successful_http}/154 |\n| Kecocokan kategori intent | ${(100*baseline.metrics.intent_accuracy).toFixed(2)}% | ${(100*ai.metrics.intent_accuracy).toFixed(2)}% |\n| Jawaban dengan evidence | ${baseline.metrics.answers_with_evidence} | ${ai.metrics.answers_with_evidence} |\n| p50 | ${baseline.metrics.p50_ms} ms | ${ai.metrics.p50_ms} ms |\n| p95 | ${baseline.metrics.p95_ms} ms | ${ai.metrics.p95_ms} ms |\n\nEvaluator mencocokkan kelompok intent dengan regex kompatibilitas, bukan menilai kebenaran semantik semua jawaban. Pertanyaan keluar akun dipetakan PROFILE (perbedaan kategori). Gap panduan penggunaan yang sebelumnya dipetakan SEARCH_PLACE diperbaiki dan diuji ulang melalui unit serta HTTP. Jumlah evidence tercatat di tabel; adanya evidence bukan bukti semua jawaban benar.\n\nHallucination rate dan groundedness = null (belum diukur melalui review klaim). Latensi dipengaruhi cache, provider dan mesin; tidak ada kesimpulan peningkatan 100×. Tes offline 154 pertanyaan menguji classifier saja.\n\nRouter eksplisit: SEARCH_PLACE, NEARBY, ROUTE, TRANSIT, UMKM, PROMOTION, CCTV, SENSOR, WEATHER, EARTHQUAKE, FIRE, AIR_QUALITY, FLOOD, ACCESSIBILITY, COMMUNITY, ADMIN, GENERAL_HELP dan INTERNATIONAL. Komposit data internasional menjalankan maksimal tiga tool. CCTV menjawab inventory/tautan resmi; sensor tanpa data menjawab tidak tersedia. GIS tidak diganti narasi model.\n`,
'SECURITY.md':`# Keamanan dan aksesibilitas\n\n${common}\n## Pemeriksaan HTTP\n\n${security.checks.map(c=>`- ${c.name}: HTTP ${c.status}, ${c.passed?'PASS':'FAIL'}.`).join('\n')}\n\nTidak ada perubahan pembayaran, klaim merchant atau moderasi melalui pengujian read-only ini. Regresi unit mencakup autentikasi, RBAC/RLS/ownership yang sudah tersedia; ini bukan audit penetration testing menyeluruh.\n\n## Kontrol data provider\n\nValidasi public URL/DNS membatasi SSRF. Body provider dibatasi 8 MB, body interpretasi 16 KB. Timeout/retry/circuit breaker membatasi kegagalan upstream. Log memuat request ID, provider ID, endpoint, latency, status, error code, cache, quality dan count; tidak menyertakan token atau body request. Secrets tetap environment server.\n\nCSP frontend memakai allowlist iframe kamera resmi, frame-ancestors none, object-src none, nosniff dan DENY. CSP aplikasi masih mempunyai unsafe-inline/unsafe-eval yang diwarisi; belum merupakan nonce-based CSP penuh. Rate limiter/cache masih per proses.\n\n## Aksesibilitas\n\nPlaywright + axe-core pada viewport 390×844: login, weather dan CCTV, ${accessibility.passed?'0 pelanggaran terdeteksi':'ada kegagalan'}. Nama tombol ikon diperjelas; nested button kamera dihapus; teks/placeholder diberi kontras dan fokus terlihat. Bukti ini hanya subset otomatis WCAG 2.1 AA, bukan sertifikasi seluruh aplikasi atau iframe provider.\n`,
'DEPLOYMENT.md':`# Deployment dan rollback\n\n${common}\n## Isolasi\n\nWorktree D:/Getra_Hardening, terpisah dari D:/Getra_Production yang sedang dikerjakan proses lain. Snapshot base d029708; branch hardening tidak melakukan reset/clean/force-push terhadap pekerjaan lain.\n\nVM kandidat: /home/getra/getra-hardening-20260920, compose project getra-hardening; frontend 127.0.0.1:3100 dan backend 127.0.0.1:8180. Produksi tetap frontend3003/backend3002 melalui Tailscale URL kanonis. Node22 image dipin digest; image revision berisi SHA kode. Compose kandidat memakai routing network getra_default dan secret env yang sudah ada.\n\nBuild: ${release.build_status}. Kandidat: ${release.candidate_status}. Promoted: ${release.promoted}.\n\n## Prosedur operator\n\n1. git fetch --all --prune; audit branch, status, remote dan runtime revision.\n2. Buat archive SHA tervalidasi; jangan menyalin node_modules, .env atau output browser ke build context.\n3. Build frontend/backend dari docs/qa/phase10d/compose.yml memakai GETRA_RELEASE_SHA, GETRA_FRONTEND_PORT=3100, GETRA_BACKEND_PORT=8180 dan project getra-hardening. Tambahkan override docs/production-hardening/candidate-compose.yml untuk origin SSH localhost; override ini tidak dipakai produksi.\n4. Jalankan kandidat, health check, tunnel lokal 3100/8180, lalu uji basemap, auth, source, Journey, service area, keamanan dan responsive.\n5. Lengkapi semua blocker pada QA_REPORT. Promosi hanya setelah candidate PASS dan rekonsiliasi commit deployment paralel.\n6. Simpan image/frontend-backend revision sebelumnya sebelum perubahan routing publik. Uji tiga URL kanonis kembali.\n\nRollback: gunakan kembali pasangan image produksi yang dicatat sebelum promosi, compose up -d hanya service yang berubah, lalu health/login/route smoke. Kandidat terisolasi dapat dihentikan melalui docker compose -p getra-hardening ... stop; jangan menghentikan project produksi. Pada laporan ini tidak ada promosi sehingga produksi tidak membutuhkan rollback hardening.\n`,
'QA_REPORT.md':`# QA dan release gate\n\n${common}\n## Bukti yang lulus\n\n- Frontend: 81 file, 588 tes PASS. Backend: 183 file PASS, 2 file skipped; 1.759 tes PASS, 3 skipped. Total 2.347 PASS, 0 failed.\n- Typecheck frontend/backend/root: PASS. Lint frontend/backend dengan max-warnings=0: PASS.\n- Basemap browser: OSM, Esri dan kembali MAPID dengan instance/zoom/center/route/merchant popup tetap; CARTO fallback diuji, tile CARTO belum tersedia.\n- Journey dengan GPS emulasi tetap aktif saat tiga pergantian basemap. Service-area kontur jaringan tidak hilang saat tiga pergantian.\n- Responsive: ${responsive.viewports.length} pemeriksaan pada enam viewport, ${responsive.passed?'PASS':'FAIL'}. 390×844, 430×932, 768×1024, 1024×768, 1280×800, 1440×900.\n- Keamanan HTTP: ${security.checks.filter(c=>c.passed).length}/${security.checks.length} PASS. Aksesibilitas otomatis tiga halaman: ${accessibility.passed?'PASS':'FAIL'}.\n- AI: 154 HTTP sukses; kecocokan kategori ${(ai.metrics.intent_accuracy*100).toFixed(2)}%, bukan akurasi faktual.\n\n## Gate yang belum lulus\n\n${blockers.map(b=>'- '+b).join('\n')}\n\nP0/P1 belum dapat dinyatakan nol. Tidak ada klaim READY. Kesimpulan: **NOT_READY untuk keseluruhan master work order**; perbaikan yang teruji tersedia sebagai kandidat terpisah. HTTP publik atau build sukses saja tidak menutup gate fitur.\n\n## Reproduksi\n\nnpm test; npm run typecheck; npm run lint. scripts/verify-main-basemap-browser.mjs; verify-service-area-basemap.mjs; verify-international-browser.mjs; verify-hardening-browser.mjs; verify-hardening-accessibility.mjs; verify-hardening-security.mjs; evaluate-hardening-ai.mjs. QA-only Playwright/axe dipasang dengan npm install --prefix outputs/qa-tools --no-save --package-lock=false @playwright/test @axe-core/playwright; tidak menambah dependency aplikasi. Variabel GETRA_QA_URL/API menentukan kandidat; jangan salah menguji backend produksi dari frontend kandidat.\n`,
'RELEASE_NOTES.md':`# Catatan perubahan kandidat\n\n${common}\n## Diperbaiki\n\n- Basemap pertama setelah login tidak kembali ke MAPID tanpa permintaan; layer aplikasi direhidrasi dengan dependency registry.\n- Race data peta saat style sibuk dan overlay service-area yang sebelumnya hilang.\n- Service area memakai kontur pedestrian Valhalla jika RPC graph belum siap, dengan metadata yang jujur.\n- Normalisasi kualitas, batas cache, deduplikasi, retry/circuit breaker dan logging provider.\n- 17 embed kamera spesifik; synthetic inference/metrik dihapus; sensor tanpa feed tetap unavailable.\n- Halaman konsep lama memerlukan pilihan eksplisit SIMULATION.\n- Intent CCTV/sensor/produk dan komposit international, pembatasan body dan kebijakan endpoint publik.\n- Kontras serta struktur tombol CCTV dan label aksesibilitas.\n\n## Yang belum selesai\n\n${blockers.map(b=>'- '+b).join('\n')}\n\nPDF ini melengkapi panduan international sebelumnya. Screenshot baru diberi konteks kandidat, bukan diklaim sebagai versi publik setelah promosi.\n`
};
for(const [name,body] of Object.entries(docs))await writeFile(`${root}/${name}`,body);
const dependencyNote=`\n\n## Dependency security\n\nPatch kandidat: Next.js 16.3.5, MapLibre GL 6.4.1, sharp 0.35.4 dan js-yaml 4.3.2. Backend build kembali memeriksa error TypeScript. Audit runtime: ${JSON.stringify(dependencyAudit?.metadata?.vulnerabilities??'PENDING')}. Audit seluruh dependency termasuk tooling: ${JSON.stringify(dependencyAuditAll?.metadata?.vulnerabilities??'PENDING')}. Temuan tooling harus ditinjau terpisah; tidak dinyatakan sebagai endpoint produksi.\n\nSumber maintainer: [Next.js Windows RCE](https://github.com/vercel/next.js/security/advisories/GHSA-p293-qw3h-jr36), [Next.js image optimization](https://github.com/advisories/GHSA-2xp9-vwfh-vxw4), [MapLibre sanitizer](https://github.com/maplibre/maplibre-gl-js/security/advisories/GHSA-jrc7-96c5-q579), [sharp](https://github.com/advisories/GHSA-rgj7-g3m4-5g8c), [js-yaml](https://github.com/advisories/GHSA-2883-xcg3-v3hh).\n`;
docs['SECURITY.md']+=dependencyNote;await writeFile(`${root}/SECURITY.md`,docs['SECURITY.md']);
for(const [name,data] of [['dependency-audit',dependencyAudit],['dependency-audit-all',dependencyAuditAll]])if(data)await writeFile(`${root}/evidence/${name}.json`,JSON.stringify(data,null,2));
const vmRuntime=await optional('outputs/production-hardening/vm-runtime/report.json');
if(vmRuntime){await writeFile(`${root}/evidence/vm-runtime.json`,JSON.stringify(vmRuntime,null,2));docs['QA_REPORT.md']+=`\n\n## Uji image produksi di VM\n\nImage frontend/backend memakai SHA ${release.release_sha}. Base localhost:3100/8180 pada laporan VM adalah SSH tunnel ke kandidat production build, bukan next dev. Runtime smoke: ${vmRuntime.passed?'PASS':'FAIL'}; tiga URL kandidat HTTP 200, walking/motorcycle/car ROUTABLE, AI Vision UNAVAILABLE tanpa canvas sintetis.\n\nDaftar sumber dapat bertambah setelah efek UI selesai memuat. Pemeriksaan overlay membandingkan setiap source awal menurut ID, jumlah record dan geometri rute, sambil mengizinkan source baru yang tidak menghapus overlay lama. Service area juga diuji benar-benar rendered pada zoom 15.\n`;await writeFile(`${root}/QA_REPORT.md`,docs['QA_REPORT.md']);}
for(const [name,value] of Object.entries({responsive,accessibility,security,'provider-smoke':api,'ai-summary':{baseline:baseline.metrics,candidate:ai.metrics,method:ai.kind,mismatches:ai.results.filter(r=>!r.intent_match).map(r=>({id:r.id,question:r.question,expected:r.expected_intent,actual:r.intent}))},'public-smoke':publicAudit}))if(value)await writeFile(`${root}/evidence/${name}.json`,JSON.stringify(value,null,2));
for(const type of ['main-map','journey','service-area'])await cp(`outputs/international-audit/${type}/report.json`,`${root}/evidence/${type}.json`);
const manifest={generated_at:date,status:'NOT_READY',release_sha:release.release_sha,branch:'fix/production-hardening-20260920',build_status:release.build_status,test_count:2347,failed_test_count:0,skipped_test_count:3,public_url:canonical,health_url:'https://getra-routing-api.tail0ed517.ts.net/api/health',runtime_status:release,camera_count:20,verified_embed_count:camera.results.filter(r=>r.http===200).length,sensor_count:9,live_sensor_count:0,provider_status:api.results.map(r=>({layer:r.layer,status:r.status,count:r.count})),international_feature_status:api.results,AI_eval_result:ai.metrics,responsive_result:{passed:responsive.passed,checks:responsive.viewports.length},blockers};
await writeFile(`${root}/release-status.json`,JSON.stringify(manifest,null,2));
const shots=[['outputs/international-audit/main-map/mapid-default.png','mapid.png','MAPID, rute dan marker merchant — kandidat lokal'],['outputs/international-audit/service-area/mapid-default.png','service-area.png','Kontur jaringan pedestrian — kandidat lokal'],['outputs/production-hardening/responsive/_international_cctv-1440.png','cctv-desktop.png','CCTV: registry dan player resmi — kandidat lokal'],['outputs/production-hardening/responsive/cctv-AI-VISION-1440.png','ai-vision.png','AI Vision: unavailable tanpa inference — kandidat lokal'],['outputs/production-hardening/responsive/cctv-SENSORS-1440.png','sensors.png','Sensor: data unavailable — kandidat lokal'],['outputs/production-hardening/accessibility/_international_cctv.png','cctv-mobile.png','CCTV mobile setelah perbaikan kontras — kandidat lokal'],['outputs/production-hardening/responsive/_international_weather-390.png','weather-mobile.png','Cuaca mobile: pilih lokasi lalu muat sumber — kandidat lokal']];
const figures=[];for(const [original,to,originalCaption]of shots){let from=original.replace('/responsive/','/vm-responsive/').replace('/accessibility/','/vm-accessibility/'),caption=originalCaption.replace('kandidat lokal','kandidat VM');try{await cp(from,`${root}/screenshots/${to}`);const data=await readFile(from);figures.push(`<section class="shot"><h2>${caption}</h2><img src="data:image/png;base64,${data.toString('base64')}"/><p>${caption}. Gambar merupakan bukti saat pengujian, bukan jaminan ketersediaan provider saat berikutnya.</p></section>`);}catch{console.log('Screenshot not found',from);}}
try{const file='outputs/production-hardening/vm-responsive/camera-player.png',data=await readFile(file);await cp(file,`${root}/screenshots/camera-player.png`);await cp('outputs/production-hardening/vm-responsive/camera-player.json',`${root}/evidence/camera-player.json`);figures.push(`<section class="shot"><h2>Player kamera resmi — kandidat VM</h2><img src="data:image/png;base64,${data.toString('base64')}"/><p>Frame yang benar-benar diputar oleh player provider. UNKNOWN adalah status registry yang belum memiliki health check berkala; tampilan ini tidak membuktikan inference AI tersedia.</p></section>`);}catch{console.log('Camera player capture unavailable');}
const html=`<!doctype html><html lang="id"><meta charset="utf-8"><title>GETRA Production Hardening</title><style>@page{size:A4;margin:18mm 16mm 18mm}body{font:11px/1.65 Arial,sans-serif;color:#24354a}h1{font-size:25px;color:#086a7b;border-bottom:3px solid #0e8995;padding-bottom:10px}h2{font-size:16px;color:#075c6b;margin-top:24px}h3{font-size:13px}a{color:#066784;overflow-wrap:anywhere}table{border-collapse:collapse;width:100%;font-size:9px}th,td{padding:6px;border:1px solid #d3e0e5;text-align:left}th{background:#e7f3f4}code{font-size:9px;overflow-wrap:anywhere}article,.shot{break-before:page}article:first-child{break-before:auto}li{margin:5px 0}img{max-width:100%;max-height:230mm;object-fit:contain}h1,h2,h3{break-after:avoid}tr{break-inside:avoid}.cover{height:250mm;display:flex;flex-direction:column;justify-content:center}.status{font-size:20px;color:#9b3419;font-weight:bold}.shot{text-align:center}.shot p{font-size:10px}</style><body><div class="cover"><p>GETRA / PRODUCTION ENGINEERING</p><h1>Perbaikan Produksi & Panduan Penggunaan</h1><p>Basemap · GIS · Data internasional · CCTV · AI · Sensor</p><p class="status">NOT_READY — penerimaan penuh belum terpenuhi</p><p>${date}<br>Kandidat kode ${release.release_sha}<br>Build ${release.build_status}</p><p><a href="${canonical}">${canonical}</a></p><p>Dokumen ini memuat perubahan, cara penggunaan, screenshot, hasil pengujian, dan blocker yang masih memerlukan sumber/kredensial serta verifikasi tambahan.</p></div>${Object.values(docs).map(md=>`<article>${marked.parse(md)}</article>`).join('')}${figures.join('')}</body></html>`;
await writeFile(`${root}/GETRA_PRODUCTION_HARDENING.html`,html);
const browser=await puppeteer.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true,args:['--no-sandbox']});
try{const page=await browser.newPage();await page.setContent(html,{waitUntil:'load'});await page.pdf({path:`${root}/GETRA_PRODUCTION_HARDENING.pdf`,format:'A4',printBackground:true,displayHeaderFooter:true,headerTemplate:'<span></span>',footerTemplate:'<div style="font-size:8px;width:100%;padding:0 16mm;color:#64748b;display:flex;justify-content:space-between"><span>GETRA • Kandidat produksi • Bukti dan batasan</span><span><span class="pageNumber"></span> / <span class="totalPages"></span></span></div>'});}finally{await browser.close();}
await cp(`${root}/GETRA_PRODUCTION_HARDENING.pdf`,`${dest}/GETRA_PRODUCTION_HARDENING.pdf`);
await cp(root,`${dest}/production-hardening`,{recursive:true});
console.log(JSON.stringify({pdf:`${dest}/GETRA_PRODUCTION_HARDENING.pdf`,documents:Object.keys(docs).length,status:manifest.status,revision:execFileSync('git',['rev-parse','--short','HEAD'],{encoding:'utf8'}).trim()}));
